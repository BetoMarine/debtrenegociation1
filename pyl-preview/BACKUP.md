# Previous /pyl/ shelf backup

The live GitHub Pages shelf *before* this Fix-first DRAFT story is saved as
`pyl-preview/index.previous.html` (commit `aed68ee` on `main`).

Restore without touching the three product apps:

```bash
cp pyl-preview/index.previous.html public/pyl/index.html
cp pyl-preview/index.previous.html pyl-preview/index.html
```

This DRAFT is for QA at `/pyl/` only. Production `planyourlife.tech` is not swapped.
