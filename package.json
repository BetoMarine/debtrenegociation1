{
  "name": "right-door",
  "version": "0.9.0",
  "private": true,
  "description": "Right Door, Sunday Pack, and Fortune Teller — three separate on-device HK tools from Plan Your Life",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "jspdf": "^3.0.1"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vite-plugin-pwa": "^0.21.1",
    "vitest": "^3.0.8"
  }
}
